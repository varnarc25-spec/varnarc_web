# Construction Intelligence seed

Entry: `seedConstructionIntelligence()` from `packages/database/src/seed.ts`.

Idempotent upserts by slug/code.

Creates: India + states/UTs + major cities; CPWD/Varnarc **source registry**; labour trades; equipment; professional services; 23 phase templates; core work items; wastage rules; quality tiers; interior components; one national independent-house benchmark ₹/sq ft (`ESTIMATED_FALLBACK`).

Does **not** create official city material prices.
