/** @deprecated Use `./intent-navigator` — kept so existing imports keep working. */
export {
  ConstructionIntentNavigator,
  ConstructionIntentCards,
} from '@/components/construction/landing/intent-navigator';
